// Where the "Enter command center" buttons go. On Vercel, set VITE_DASHBOARD_URL
// in the splash project's environment variables to the dashboard's deployed URL.
export const DASHBOARD_URL = import.meta.env.VITE_DASHBOARD_URL || 'http://localhost:3000/dashboard';

export const REPO_URL = 'https://github.com/sirgreaseball/cascade';
