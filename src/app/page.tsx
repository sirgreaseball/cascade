import Dashboard from '@/components/Dashboard';

// The dashboard also lives at /dashboard, so a marketing splash page can take over "/" later.
export default function Home() {
  return <Dashboard />;
}
